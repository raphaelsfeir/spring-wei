import { SwipeTabDirective } from './swipe-tab.directive';

describe('SwipeTabDirective', () => {
  it('should create an instance', () => {
    const directive = new SwipeTabDirective();
    expect(directive).toBeTruthy();
  });
});
